import DefaultEasyFinance, { EasyFinance } from '../index';

describe('EasyFinance', () => {
  describe('default branch', () => {
    test('should ', () => {
      expect(DefaultEasyFinance).toBeDefined();
    });
  });
  describe('branch 1', () => {
    test('should', () => {
      expect(EasyFinance).toBeDefined();
    });
  });
});
